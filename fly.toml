# fly.toml file generated for astromonkey on 2023-04-17T14:32:14+09:00

app = "astromonkey"
primary_region = "ord"

[build]
  builder = "heroku/buildpacks:20"

[env]
  PORT = "8080"

[http_service]
  internal_port = 8080
  force_https = true

[checks]
  [checks.alive]
    type = "tcp"
    interval = "15s"
    timeout = "2s"
    grace_period = "5s"
